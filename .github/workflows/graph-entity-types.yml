name: graph-entity-types CI
on:
  push:
    branches: [main]
    paths:
      - "packages/graph-entity-types/**"
      - ".github/workflows/graph-entity-types.yml"
      - ".github/workflows/module.yml"

permissions:
  id-token: write
  contents: read

jobs:
  module-flow:
    uses: ./.github/workflows/module.yml
    with:
      package-path: packages/graph-entity-types
